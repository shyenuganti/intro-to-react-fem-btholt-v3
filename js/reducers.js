const DEFAULT_STATE = {
    searchTerm = ''
};

const rootReducer = (state = DEFAULT_STATE, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}

export default rootReducer;